# AI Admin Minimal Enterprise UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reduce the AI Admin surface to a calmer enterprise permission-governance console with one primary approval/acceptance path.

**Architecture:** Keep backend and permission semantics unchanged. Use i18n and the tested production summary model to drive a simpler UI: a status rail, one primary action, and collapsed secondary sections. Replace AI-coded visual cues with enterprise governance iconography and quieter colors.

**Tech Stack:** React 19, TypeScript, Vite, Node test runner, existing AgentHarbor i18n dictionary.

---

### Task 1: Copy And Summary Model

**Files:**
- Modify: `frontend/tests/i18n.test.mjs`
- Modify: `frontend/src/i18n.ts`
- Modify: `frontend/tests/aiAdminProductionConsole.test.mjs`
- Modify: `frontend/src/aiAdminProductionConsole.ts`

- [x] Add failing i18n assertions for concise enterprise labels: Permission Packages nav, permission-package approval page title, production primary action, local development mode wording.
- [x] Add failing production summary assertion that the top path has five user-facing steps instead of six cards.
- [x] Run focused frontend tests and verify failures.
- [x] Update translations and the summary model.
- [x] Run focused frontend tests and verify pass.

### Task 2: Minimal Layout And Visual Language

**Files:**
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/styles.css`

- [x] Replace Sparkles on AI Admin with ShieldCheck.
- [x] Render production status as a compact rail instead of six colored cards.
- [x] Keep exactly one primary action in the production header; keep detailed configuration, evidence, and tenant profile collapsed or visually secondary.
- [x] Make visual styling quieter: neutral surfaces, thinner borders, less shadow, status colors only on rail markers.
- [x] Run `pnpm --dir frontend build`.

### Task 3: Docs And Verification

**Files:**
- Modify: `README.md`
- Modify: `docs/product/0.2.0-ai-admin-permission-journey.md`
- Modify: `CHANGELOG.md`

- [x] Update bilingual docs to describe the simplified enterprise permission-package console.
- [x] Run `git diff --check`.
- [x] Run `pnpm --dir frontend test -- --run`.
- [x] Run `make check`.
- [x] Verify in the in-app browser that Permission Packages is Chinese, has one primary visible action, uses a compact status rail, has no old AI/demo primary wording, and console errors are empty.
