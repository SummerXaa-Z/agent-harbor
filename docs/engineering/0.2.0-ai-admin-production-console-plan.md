# AI Admin Production Console Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the AI Admin permission-package surface feel like a production B2B control plane rather than a demo journey runner.

**Architecture:** Keep backend behavior unchanged. Add a small tested frontend summary model, then use it to render a production decision strip above the existing evidence controls. Rename demo-oriented copy in English and Simplified Chinese, and document the UI positioning.

**Tech Stack:** React 19, TypeScript, Vite, Node test runner, existing AgentHarbor i18n dictionary.

---

### Task 1: Production Summary Model

**Files:**
- Create: `frontend/src/aiAdminProductionConsole.ts`
- Create: `frontend/tests/aiAdminProductionConsole.test.mjs`

- [x] Add failing tests for the production decision summary: draft scope, approval state, application state, runtime evidence, and production readiness.
- [x] Run `pnpm --dir frontend test -- --run frontend/tests/aiAdminProductionConsole.test.mjs` and verify it fails because the module is missing.
- [x] Implement `buildAiAdminProductionConsoleSummary` as a pure function with no React dependency.
- [x] Run the focused test and verify it passes.

### Task 2: AI Admin Page Information Architecture

**Files:**
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/styles.css`
- Modify: `frontend/tests/i18n.test.mjs`
- Modify: `frontend/src/i18n.ts`

- [x] Add failing i18n assertions for production-console wording and remove demo/workbench phrasing from primary AI Admin labels.
- [x] Run `pnpm --dir frontend test -- --run frontend/tests/i18n.test.mjs` and verify it fails.
- [x] Import the summary helper into `App.tsx` and render a top `ai-admin-production-console` strip before detailed evidence.
- [x] Rename primary AI Admin actions and sections to production language in English and Simplified Chinese.
- [x] Update CSS so AI Admin starts with a dense status strip, primary actions are grouped, and detailed readiness/evidence stays visually secondary.
- [x] Run focused frontend tests and `pnpm --dir frontend build`.

### Task 3: Product Documentation And Verification

**Files:**
- Modify: `README.md`
- Modify: `docs/product/0.2.0-ai-admin-permission-journey.md`
- Modify: `CHANGELOG.md`

- [x] Update bilingual docs to describe the AI Admin surface as a production approval and acceptance console.
- [x] Run `git diff --check`.
- [x] Run `pnpm --dir frontend test -- --run`.
- [x] Run `make check` or the focused frontend gate if backend is unchanged.
- [x] Verify in the in-app browser that AI Admin is Chinese, the first screen presents production state rather than demo wording, and the console has no errors.
