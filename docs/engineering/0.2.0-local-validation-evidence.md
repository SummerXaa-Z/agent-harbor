# AgentHarbor 0.2.0 Local Validation Evidence

This note records local validation evidence for the current v0.2.0 permission-package production journey work.

本文记录当前 v0.2.0 权限包生产旅程工作的本地验收证据。

## Validation Date / 验收日期

- Date: 2026-06-08
- Local workspace: `/Users/gugu/Documents/code-xt/agent-harbor`

## Release Gate / 发布门禁

Command:

```bash
make release-check
```

Result:

- Passed uncached Go tests with `go test -count=1 ./...`.
- Passed `go vet ./...` and `go build ./...`.
- Passed production hardening baseline: public health remains open, management APIs reject missing or wrong admin keys, permission-package and management MCP endpoints require the configured admin key, loopback/private MCP upstreams are rejected by default, and public HTTPS MCP target registration still works.
- Passed frontend unit tests: 44 tests, 0 failures.
- Passed frontend production build.
- Passed scenario shell syntax checks and GitHub YAML parse checks.

结果：

- 通过未缓存 Go 测试：`go test -count=1 ./...`。
- 通过 `go vet ./...` 和 `go build ./...`。
- 通过生产安全基线：健康检查保持公开，管理 API 拒绝缺失或错误的 admin key，权限包和 management MCP 端点要求配置的 admin key，默认拒绝 loopback/private MCP 上游，公共 HTTPS MCP 目标仍可注册。
- 通过前端单元测试：44 项测试，0 失败。
- 通过前端生产构建。
- 通过场景脚本语法检查和 GitHub YAML 解析检查。

## Primary Permission Journey / 主权限旅程

Command:

```bash
AGENT_HARBOR_ALLOW_PRIVATE_UPSTREAMS=true make scenario-permission-package-approval
```

Run evidence:

- Run ID: `permission-package-approval-20260608164630`
- Subject ID: `user:support-001`
- MCP endpoint: `http://127.0.0.1:8787/mcp`
- The scenario created a three-level tenant tree for the run.
- MCP capability discovery found `search_customer`, `update_ticket`, and `export_contracts`.
- The support ticket triage draft required approval and direct apply without approval was rejected.
- Apply preflight reported missing approval before approval and ready after approval.
- Production readiness and production evidence report were blocked before application evidence existed.
- The approved permission package was applied and the approval request was consumed.
- Reusing the consumed approval request was rejected.
- Runtime verification denied `export_contracts` and allowed `update_ticket`.
- Denied and allowed trace evidence was recorded.
- Tenant access profile showed the approval grant chain.
- Permission package application list, health, impact, drift rehearsal, and audit evidence checks passed.
- Final production readiness and production evidence report returned `status=ready`.

运行证据：

- Run ID：`permission-package-approval-20260608164630`
- 主体 ID：`user:support-001`
- MCP 端点：`http://127.0.0.1:8787/mcp`
- 场景为本次运行创建了三级租户树。
- MCP 能力发现识别出 `search_customer`、`update_ticket` 和 `export_contracts`。
- 客服工单处理草案需要审批，未审批直接应用被拒绝。
- 应用前预检在审批前报告缺失审批，审批后报告可应用。
- 在应用证据存在前，上线验收和上线证据报告均为 blocked。
- 已审批权限包成功应用，审批请求被消费。
- 复用已消费审批请求被拒绝。
- 运行验证拒绝 `export_contracts`，允许 `update_ticket`。
- 拒绝和允许追踪证据均已记录。
- 租户访问画像展示审批授权链。
- 权限包应用列表、落地状态、影响复核、漂移演练和审计证据检查通过。
- 最终上线验收和上线证据报告返回 `status=ready`。

## Browser-Facing AI Admin Gate / 浏览器侧 AI Admin 门禁

Command:

```bash
AGENT_HARBOR_BROWSER_GATE_API_PORT=19090 \
AGENT_HARBOR_BROWSER_GATE_FRONTEND_PORT=15174 \
MOCK_MCP_PORT=18787 \
  make ai-admin-browser-journey
```

Run evidence:

- Run ID: `ai-admin-browser-journey-20260608165340`
- API: `http://127.0.0.1:19090`
- Web console: `http://127.0.0.1:15174`
- Mock MCP: `http://127.0.0.1:18787/mcp`
- Browser CORS preflight allowed `X-AgentHarbor-Subject-Id`.
- The browser-facing gate ran the same approval-required permission package scenario against the isolated live API, Mock MCP, and web console stack.
- Final production readiness and production evidence report returned `status=ready`.

运行证据：

- Run ID：`ai-admin-browser-journey-20260608165340`
- API：`http://127.0.0.1:19090`
- Web 控制台：`http://127.0.0.1:15174`
- Mock MCP：`http://127.0.0.1:18787/mcp`
- 浏览器 CORS 预检允许 `X-AgentHarbor-Subject-Id`。
- 浏览器侧门禁在隔离的实时 API、Mock MCP 和 Web 控制台栈上运行了同一条需要审批的权限包场景。
- 最终上线验收和上线证据报告返回 `status=ready`。

## Remaining External Evidence / 仍需外部证据

The current evidence is local and memory-backed unless PostgreSQL is configured separately. Before a tagged release or downstream customer handoff, also collect:

- PostgreSQL integration evidence with `make test-postgres` when `AGENT_HARBOR_TEST_DATABASE_URL` is available.
- PR or main-branch CI evidence after the changes are committed and pushed.
- Any customer-specific MCP endpoint and credential validation outside the local mock MCP path.

当前证据为本地证据，且除非单独配置 PostgreSQL，否则以 memory-backed 运行时为主。发布标签或对外交付前，还需要补充：

- 配置 `AGENT_HARBOR_TEST_DATABASE_URL` 后的 `make test-postgres` 证据。
- 变更提交并推送后的 PR 或 main 分支 CI 证据。
- 本地 mock MCP 路径之外的客户真实 MCP 端点和凭据验证证据。
