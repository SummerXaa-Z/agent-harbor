# 0.2.0 Management MCP Catalog Diagnostics

Date: 2026-07-07
Branch: `codex/management-mcp-catalog-diagnostics`

## Objective

Expose the management MCP tool-catalog contract in the web console connection diagnostics, so operators can detect incompatible admin-agent tooling before automated write calls.

## 中文说明

上一轮已经让 `tools/list` 返回 `metadataVersion: 1`，并为每个管理工具提供 `safety` 和 `access` 元数据。本轮把这个契约接入 Web 控制台的连接诊断：管理员运行诊断时，不只看到 API、实时数据和 MCP 工具服务是否可用，也能看到 Management MCP 管理工具目录是否具备当前 Agent 自动配置所需的安全与权限元数据。

## Behavior

- `metadataVersion: 1` with complete `tools[].safety` and `tools[].access` returns a ready diagnostic row.
- Missing version, empty catalog, or incomplete metadata returns a blocking diagnostic row.
- Unknown future metadata versions return a warning row, because automated write calls should pause for compatibility review.
- The hook reads the console session first so browser-session CSRF state is available before calling the management MCP endpoint.
- The decision logic lives in `frontend/src/connectionDiagnostics.ts`; the hook only fetches data and maps failures into diagnostic input.

## Changes

- Added management MCP catalog diagnostics to the connection-diagnostics model.
- Added `fetchManagementMcpToolsCatalog` for the browser console.
- Wired `useConnectionDiagnostics` to call management MCP `tools/list` after session refresh.
- Added English and Simplified Chinese copy for the new diagnostic row.
- Extracted the backend `metadataVersion` value into a constant.
- Updated product docs and changelog in English and Simplified Chinese.

## Verification

- Red: `PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH pnpm --dir frontend exec node --test tests/connectionDiagnostics.test.mjs` failed while connection diagnostics still rendered only four rows.
- Passed: `PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH pnpm --dir frontend exec node --test tests/connectionDiagnostics.test.mjs tests/i18n.test.mjs`.
- Passed: `GOCACHE=/private/tmp/agent-harbor-go-cache go test ./internal/httpapi -run TestManagementMCPToolsListAndPermissionPackageCalls -count=1`.
- Passed: `PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH pnpm --dir frontend build`.
- Passed: `GOCACHE=/private/tmp/agent-harbor-go-cache go test ./internal/httpapi ./internal/domain ./internal/store -count=1`.
- Passed: `PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH pnpm --dir frontend test`.
- Passed: `bash -n scripts/scenario-permission-package-approval.sh scripts/scenario-web-console-production-journey.sh`.
- Passed: `AGENT_HARBOR_PERMISSION_PACKAGE_API_PORT=9307 MOCK_MCP_PORT=8907 GOCACHE=/private/tmp/agent-harbor-go-cache make scenario-permission-package-approval`.
- Passed: `PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH GOCACHE=/private/tmp/agent-harbor-go-cache bash scripts/scenario-web-console-production-journey.sh`.
- Passed: `git diff --check`.
- Passed: `CI=true PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH GOCACHE=/private/tmp/agent-harbor-go-cache make check`.
- Passed: `CI=true PATH=/Users/gugu/Library/pnpm/.tools/pnpm/10.30.3/bin:$PATH GOCACHE=/private/tmp/agent-harbor-go-cache make release-check`.

Notes:

- A direct `bash scripts/scenario-web-console-production-journey.sh` run failed locally because the shell picked bundled pnpm 11.7.0 while the frontend requires `>=10 <11`. Re-running with the repo's pnpm 10.30.3 path passed.
- Local frontend commands still emit the existing Node engine warning because this machine runs Node v26.3.0 while the frontend package declares `>=24 <25`.
- Vite still emits the existing large chunk warning; build exits successfully.
