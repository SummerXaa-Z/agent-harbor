# 0.2.0 Production Storage Preflight Warning

Date: 2026-06-17
Branch: `codex/production-storage-preflight-warning`

## Objective

Make production-mode startup visibly warn when AgentHarbor is running without persistent database storage.

## 中文说明

权限平台的租户、Agent、授权、凭据和审计记录不能在生产中依赖内存仓库。直接把缺少 `AGENT_HARBOR_DATABASE_URL` 改成阻断项会让当前本地 release gate 必须依赖 PostgreSQL，因此本轮先把它纳入生产部署预检 warning，并在启动时输出结构化 warning 日志。这样部署者能在上线前看到持久化存储缺口，而本地无依赖验证仍然可运行。

## Scope

- Backend deployment preflight model.
- Startup logging for warning-level deployment checks.
- Production hardening scenario assertion.
- No API route contract changes.
- No frontend changes or new dependencies.

## Changes

- Added `AGENT_HARBOR_DATABASE_URL` to deployment preflight input.
- Added `persistent_storage_configured` production check:
  - `warning` when production mode uses the in-memory repository.
  - `passed` when a database URL is configured.
- Added structured warning logs for warning-level deployment checks.
- Updated `scenario-production-hardening.sh` to assert the production safe-minimal startup emits the persistent-storage warning.
- Documented the production storage warning in README runtime configuration.

## Verification

- Red: `go test ./internal/app -run 'TestDeploymentConfigPreflight.*ProductionStorage|TestDeploymentConfigPreflightAcceptsProductionDatabaseStorage' -count=1` failed because the preflight had no persistent-storage check.
- Passed: `go test ./internal/app -run 'TestDeploymentConfigPreflight.*ProductionStorage|TestDeploymentConfigPreflightAcceptsProductionDatabaseStorage' -count=1`
- Passed: `go test ./internal/app -count=1`
- Passed: `bash scripts/scenario-production-hardening.sh`
- Passed: `make check`
- Passed: `make release-check`

## Notes

This intentionally remains a warning rather than a blocker in this slice. A later deployment profile can make persistent storage blocking once the release gate has a PostgreSQL-backed production-mode path.
